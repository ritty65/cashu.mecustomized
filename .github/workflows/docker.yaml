name: Docker Build
on:
  pull_request:
    branches: [ Phase10 ]
  push:
    branches: [ "**" ]
jobs:
  check-secrets:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No secrets to check for this web-only PR."
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No Dockerfile present; Docker build intentionally skipped."
